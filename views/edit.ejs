<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/info.css" />
  <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
  <script defer src="/edit.js"></script>
  <title>edit</title>
</head>

<body>
  <div id="wrap">
    <header class="header">
      <h1>today</h1>
    </header>
    <div class="container">
      <div class="wrap">
        <form enctype="multipart/form-data" id="edit_form">
          <div class="profile_wrap">
            <div class="preview_wrap">
              <% if (data.img == "") { %>
              <img id="preview_img" src="profile.jpg">
              <% } else { %>
              <img id="preview_img" src="/uploads/<%= data.img %>">
              <% } %>
            </div>
            <div class="input_wrap">
              <label for="img_input" id="img_label">프로필 이미지 업로드</label>
              <input type="file" name="image" id="img_input">
            </div>
          </div>
          <fieldset>
            <dl>
              <dt>
                <label>아이디</label>
              </dt>
              <dd>
                <input type="text" id="id" name="id" class="input_info" placeholder="ID" value="<%= data.id %>"
                  autocomplete="off">
                <button type="button" id="check_button" value="checked">중복 확인</button>
              </dd>
            </dl>
            <dl>
              <dt>
                <label>비밀번호</label>
              </dt>
              <dd>
                <input type="password" id="pw" name="pw" class="input_info" placeholder="Password"
                  value="<%= data.pw %>" autocomplete="off">
              </dd>
            </dl>
            <dl>
              <dt>
                <label>이름</label>
              </dt>
              <dd>
                <input type="text" id="name" name="name" class="input_info" placeholder="Name" value="<%= data.name %>"
                  autocomplete="off">
              </dd>
            </dl>
            <button type="button" id="submit_button">내 정보 수정</button>
          </fieldset>
        </form>
      </div>
    </div>
  </div>
</body>

</html>